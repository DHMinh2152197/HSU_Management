<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minhd.hsumanagement">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/img_logo_hsu_word"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ControlActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".ScoreActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".ExamDateActivity" />
        <activity android:name=".CheckAttendanceActivity" />
        <activity android:name=".TuitionActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".PhotoAccountActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".ContactActivity"></activity>
    </application>

</manifest>